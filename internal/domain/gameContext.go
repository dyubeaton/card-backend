// handles the broad context of the game, everything outside of the encounter
// will hold the player's deck and any changes to it
// will handle grabbing templates from the registry (maybe this can go to something else?)
package domain

type GameContext struct {
}
